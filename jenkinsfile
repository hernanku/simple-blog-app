pipeline {
    agent {
        docker {
            image 'node:13.12.0'
        }
    } 
    stages {
        stage('Code Build') {
            steps {
                echo 'Running npm build'
                sh 'npm --version'
                sh 'npm install'
            }
        }
    }
}
